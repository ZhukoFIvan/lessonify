[build]
builder = "dockerfile"
dockerfilePath = "apps/api/Dockerfile"
# Контекст — корень монорепо (нужен для workspace)
dockerContextPath = "."

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
healthcheckPath = "/health"
healthcheckTimeout = 30
